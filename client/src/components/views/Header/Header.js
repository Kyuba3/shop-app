import { Container } from "react-bootstrap";
import NavBar from '../NavBar/NavBar';

const Header = () => {
  return (
    <Container fluid>
      <NavBar />
    </Container>
  );
};

export default Header;