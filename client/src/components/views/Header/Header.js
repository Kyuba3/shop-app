import { Container } from "react-bootstrap";
import NavBar from '../NavBar/NavBar';

const Header = () => {
  return (
    <Container>
      <NavBar />
    </Container>
  );
};

export default Header;